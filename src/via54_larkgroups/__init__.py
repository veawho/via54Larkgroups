"""via54_larkgroups: a local-only Feishu (Lark) chat archive tool.

Renamed from `feishu_vault` to match GitHub repo name `via54Larkgroups`.
The tool's CLI command stays `feishu-vault` for backward compatibility.
"""

__version__ = "0.2.0"
__all__ = ["config", "oauth", "sync", "archive", "api_client"]